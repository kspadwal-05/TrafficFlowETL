# Access path (inside container)
ACCESS_DB_PATH=/app/data/input/traffic_database.accdb

# Prefer VBA JSON (if export_from_vba.json exists)
PREFER_VBA_EXPORT=1

# API
API_BASE_URL=https://httpbin.org/post
API_TOKEN=dev-token

# Fuzzy match threshold
ACCURACY_THRESHOLD=0.7
